## Module Maker AI Context (generated by `jw:ai:init` v{{ packageVersion }} — refresh: `php artisan jw:ai:init --refresh`)

- Read `.ai/summary.md` BEFORE writing any code; full context lives in `.ai/`.
- Scaffold module files ONLY via `jw:*` generators — the `module-maker` MCP server (registered in `.mcp.json`) exposes `run_generator`, `list_modules`, `module_structure`, `generator_info`, `get_guidelines`.
- Hard rules: features live in modules (never app-level dirs); services hold logic; FormRequests validate; Resources shape output; `$fillable` always maintained.
